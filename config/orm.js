var connection = require("connection.js");

var orm = new  orm({

});

// in this area I will be creating the orm structure for  * `selectAll()`
    //  * `insertOne()`
    //  * `updateOne()` and exporting it for use in another file.

    module.exports = orm;